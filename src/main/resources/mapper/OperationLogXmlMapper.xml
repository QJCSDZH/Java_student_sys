<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.studentsystem.mapper.OperationLogMapper">

    <insert id="saveInfo">
        INSERT INTO operation_log
        (operation, request_url, http_method, class_method, ip, token, user_id, user_name, params, result, cost_time, create_time)
        VALUES
        (#{operation}, #{requestUrl}, #{httpMethod}, #{classMethod}, #{ip}, #{token}, #{userId}, #{userName}, #{params}, #{result}, #{costTime}, #{createTime})
    </insert>

</mapper>